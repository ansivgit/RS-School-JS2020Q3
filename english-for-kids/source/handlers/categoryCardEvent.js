import cardWordsCreate from '../creations/cardWordsCreate';

function categoryCardEvent(categoryObj) {
  // const activeCategory = card.getAttribute('data-category');
  // let currentCategory = {};
  // console.log(activeCategory);

  // this.data.forEach((categoryObj) => {
  //   if (categoryObj['category-name'] === activeCategory) {
  //     currentCategory = categoryObj;
  //   }
  // });

  // const categoryWords = cardWordsCreate(categoryObj);

  // this.clear();
  // this.cards.append(categoryWords);

  // return this.cards;
}

export default categoryCardEvent;
